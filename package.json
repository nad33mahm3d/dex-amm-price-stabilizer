{
  "name": "dex-amm-price-stabilizer",
  "version": "0.1.0",
  "private": false,
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --require ts-node/register ./tests/*.test.ts"
  },
  "dependencies": {
    "cli-table3": "^0.6.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.5",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
